<!DOCTYPE html>
<html>
	<head>
		<title>Blog</title>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.4.2/css/all.css" integrity="sha384-/rXc/GQVaYpyDdyxK+ecHPVYJSN9bmVFBvjA/9eOB+pb3F2w2N6fc5qB9Ew5yIns" crossorigin="anonymous">
		<link rel='stylesheet' href='/stylesheets/style.css' />
	</head>
	<body>
		<div class="card text-center">
		  <div class="card-header">
		    <h1><i class="fas fa-th-large"></i><%= username %>'s Posts</h1>
		  </div>
		  <div class="card-body">
		    <table class="table table-striped table-hover">
			  <thead>
			    <tr>
			      <th>Postid</th>
			      <th>Title</th>
			      <th>Created</th>
			      <th>Modified</th>
			      <th>Body</th>
			    </tr>
			  </thead>
			  <tbody>
			    <% for(let post of posts) { %>
			    	<tr>
			    		<td><%- post.postid -%></td>
			    		<td><%- post.title -%></td>
			    		<td><%- new Date(post.created) -%></td>
			    		<td><%- new Date(post.modified) -%></td>
			    		<td><%- post.body -%></td>
			    	</tr>
			    <% } %>
			      
			  </tbody>
			</table>
		  </div>
			  <div class="card-footer text-muted">
			  		<% if(prev != null && prev != -1) { %>
				    	<a href="/blog/<%= username %>?action=pre&start=<%= prev %>" class="btn btn-secondary btn-sm"><i class="fas fa-angle-left"></i>Previous</a>
				   	<% } %>
				    <% if(next != null) { %>
					    <a href="/blog/<%= username %>?action=next&start=<%= next %>" class="btn btn-primary btn-sm">Next<i class="fas fa-angle-right"></i></a>
					<% } %>
			  </div>
		</div>
	</body>
</html>